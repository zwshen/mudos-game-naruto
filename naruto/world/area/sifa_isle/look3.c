#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "瞭望台頂樓");
    set("long", @LONG
寬敞的頂樓，因為窗子的關係非常透氣，聞不到一點霉味。窗子前面
擺了一座巨大的望遠鏡，似乎能夠看到很遠的地方。
LONG);

    set("exits", ([
        "down" : __DIR__"look2.c",
    ]));
    set("detail", ([
        "望遠鏡": "可以選擇看四個方向(東方、西方、南方、北方)。",
        "東方": "似乎能看見七水之都的景色。",
        "西方": "霧太大了，只能勉強看到一座小島...",
        "南方": "只有一片汪洋大海和幾艘小船。",
        "北方": "能看見通往海軍總部的大門「正義之門」",
    ]));
    setup();
}
